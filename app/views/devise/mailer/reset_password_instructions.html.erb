<h1>Hello, <%= @resource.email %>!</h1>
<p>
  Someone, hopefully you, has requested the password
  for the Puzzle account <%= @resource.email %> be reset.
</p>
<p>If that sounds right, click this button:</p>
<!-- Action -->
<table class='body-action' align='center' width='100%' cellpadding='0' cellspacing='0'>
  <tr>
    <td align='center'>
      <table width='100%' border='0' cellspacing='0' cellpadding='0'>
        <tr>
          <td align='center'>
            <table border='0' cellspacing='0' cellpadding='0'>
              <tr>
                <td>
                  <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %>
                </td>
              </tr>
            </table>
          </td>
        </tr>
      </table>
    </td>
  </tr>
</table>
<p>If you have any questions, please <a href='mailto:contact@secretnodes.org'>email us</a>.</p>
<p>Thanks,
  <br>The secretnodes.org Team</p>
<!-- Sub copy -->
<table class='body-sub'>
  <tr>
    <td>
      <p class='sub'>If you’re having trouble with the button above, copy and paste the URL below into your web browser (it will only work once!).</p>
      <p class='sub'><%= link_to edit_password_url(@resource, reset_password_token: @token), edit_password_url(@resource, reset_password_token: @token) %></p>
    </td>
  </tr>
</table>
